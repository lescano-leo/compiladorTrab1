programa exemplo {
    inteiro a = 10;
    real b = 20.5;
    se (a < b) {
        escreva("a é menor que b);
    }
    // Isto é um comentário de linha
    /*
       Isto é um comentário de bloco
    */
    a = a + 1 @;
    b = 3.14.15;
    c = "string não fechada;
}